// Smoke-test the windows-vista template: boot x86_64/windows-vista on a NAT'd segment and watch the
// console. Build or pull the template first, then from the repo root:
//
//   just example-up windows-vista       # bring it up and open the console
//   just example-down windows-vista     # tear it down
//
// Hardware (cpus / memory / firmware) is inherited from the template.

import <vmlab.wcl>

lab "windows-vista" {

  segment "lan" {
    subnet = "10.99.0.0/24"
    nat    = true
  }

  vm "guest" {
    template = "x86_64/windows-vista"
    nic { segment = "lan" }
  }
}
